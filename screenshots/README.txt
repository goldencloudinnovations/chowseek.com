Replace these placeholders by adding exactly these three files:

1. search.png   — main map + search bar + a natural-language query
2. results.png  — completed search + map pins + result strip/card
3. details.png  — opened place detail showing why the result fits

Recommended capture:
- Use a modern iPhone or Android portrait simulator/device.
- Full-screen portrait screenshot; do not crop out the app chrome.
- 9:19 to 9:20-ish phone aspect ratio works best.
- Use a neutral demo location and avoid exposing personal addresses/account details.
- Keep all three screenshots from the same platform/theme for consistency.

The website automatically hides its placeholder when the matching PNG exists.
No TypeScript or HTML changes are required.
